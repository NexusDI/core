# Nexus Benchmark

This benchmark tests the performance of the Nexus DI library.

- Edit `src/index.benchmark.ts` to add your benchmark code.
- Run the benchmark with:

  nx run benchmarks-nexus:benchmark
