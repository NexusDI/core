# Security register

The classes of hostile input this package is held against, and what happens to each. The adversarial suite in `src/security` is checked against this file: one entry per class, one identifier, and the test that proves the entry. `src/security/register.test.ts` fails when an identifier appears in one place and not the other. Adding a class is an entry here plus a test there.

It runs under `nx test core`, with the rest of the package's tests.

## Tiers

- Tier 1: the container prevents it. The test asserts the hostile input produces a construction error, or no effect outside the container.
- Tier 3: out of scope. The test pins what the container actually does, so nobody reads that behaviour as a defence.

Counts: 12 tier 1, 2 tier 3.

## Tier 1: prevented

| ID      | Class                                                                                            | CWE      | Mechanism                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Test                                |
| ------- | ------------------------------------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------- |
| SEC-001 | A Token description or module name of `__proto__`, `constructor` or `prototype`                  | CWE-1321 | Names are values only. Every lookup the compiler and the runtime make is a `Map` or `WeakMap` keyed by identity, and no name becomes a property key                                                                                                                                                                                                                                                                                                                                                          | `tier1-prevented.test.ts` › SEC-001 |
| SEC-002 | Module options that carry `__proto__` or `constructor` keys through `with()` and the schema path | CWE-1321 | Options pass through as the object given, or as the schema's output, and are never merged or copied. `with()` reads `useFactory` and `deps` as own properties only                                                                                                                                                                                                                                                                                                                                           | `tier1-prevented.test.ts` › SEC-002 |
| SEC-003 | A polluted `Object.prototype` steering how a provider, a bare class or a module is read          | CWE-1321 | `provide()` options and provider literals are read as own properties: `token`, `useClass`, `useValue`, `useFactory`, `useExisting`, `deps` and `lifetime`. `@Injectable` records only the `deps` and `lifetime` its options set on themselves, and a bare class's lifetime is an own key of its metadata object. `defineModule` and `@Module` read `name`, `imports`, `providers`, `exports`, `global`, `options` and `schema` as own properties. A key that `Object.prototype` alone supplies is never read | `tier1-prevented.test.ts` › SEC-003 |
| SEC-004 | Injection metadata read from, or written to, `Object.prototype`                                  | CWE-1321 | The metadata walk stops before `Object.prototype` and reads own keys. Writes go to own keys of the class's metadata object only                                                                                                                                                                                                                                                                                                                                                                              | `tier1-prevented.test.ts` › SEC-004 |
| SEC-005 | A frozen class, prototype or instance                                                            | CWE-471  | The container keeps what it knows about a class in `WeakMap`s and writes nothing onto a class. Property injection goes through the accessor's private storage                                                                                                                                                                                                                                                                                                                                                | `tier1-prevented.test.ts` › SEC-005 |
| SEC-006 | A class behind a `Proxy`, including traps that throw                                             | CWE-248  | The proxy is its own token. A trap that throws while the compiler reads the class is reported as `NEXUS_INVALID_PROVIDER`                                                                                                                                                                                                                                                                                                                                                                                    | `tier1-prevented.test.ts` › SEC-006 |
| SEC-007 | A class whose prototype chain ends in `null`                                                     | CWE-754  | The compiler reads a class's own `name`, `length` and metadata and needs nothing from `Object.prototype` or `Function.prototype`                                                                                                                                                                                                                                                                                                                                                                             | `tier1-prevented.test.ts` › SEC-007 |
| SEC-008 | One provider entry or one module listed thousands of times                                       | CWE-407  | Entries and imports are deduplicated by identity with sets, contributions append in place, and visibility is memoised, so compile time grows linearly with the listing                                                                                                                                                                                                                                                                                                                                       | `tier1-prevented.test.ts` › SEC-008 |
| SEC-009 | Import and dependency chains 1,000 levels deep                                                   | CWE-674  | Cycle detection is iterative, re-exports are memoised, and the build walks levels in a loop                                                                                                                                                                                                                                                                                                                                                                                                                  | `tier1-prevented.test.ts` › SEC-009 |
| SEC-010 | `graph()` output for any of the above                                                            | CWE-20   | The graph is built from ids and display names only: plain objects and arrays of strings, numbers, booleans and null                                                                                                                                                                                                                                                                                                                                                                                          | `tier1-prevented.test.ts` › SEC-010 |
| SEC-011 | Global writes                                                                                    | CWE-471  | The container writes no global. The two documented polyfills define `Symbol.metadata`, `Symbol.dispose` and `Symbol.asyncDispose` when they are absent, and nothing else                                                                                                                                                                                                                                                                                                                                     | `tier1-prevented.test.ts` › SEC-011 |
| SEC-012 | A `static deps` that a throwing getter supplies, or that only a built-in prototype carries       | CWE-1321 | `static deps` is read as an own property of the class or of a parent class, stopping before `Function.prototype`. A getter that throws is `NEXUS_INVALID_PROVIDER`                                                                                                                                                                                                                                                                                                                                           | `tier1-prevented.test.ts` › SEC-012 |

## Tier 3: out of scope, documented

- SEC-201, spoofed injection metadata (CWE-345). The metadata keys are `Symbol.for` symbols, so code in the same realm can write metadata the compiler honours. Code in the same realm can also replace the classes themselves; metadata is trusted input. Test: `tier3-contract.test.ts` › SEC-201, which pins that the compiler honours hand-written metadata.
- SEC-202, a module tree deeper than the call stack (CWE-674). The module walk recurses. A chain of 1,000 levels compiles (SEC-009); a chain of 100,000 exhausts the stack with a `RangeError`, which is not a `NexusError`. No application has such a tree, so there is no defence. Test: `tier3-contract.test.ts` › SEC-202, which pins the gap.
