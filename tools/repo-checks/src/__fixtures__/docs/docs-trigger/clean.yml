name: Docs
on:
  push:
    branches: [main]
    paths:
      - 'apps/docs/**'
      - 'libs/**'
      - 'internal/**'
      - 'examples/meridian/**'
      - 'tools/doc-examples/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/docs.yml'
  workflow_dispatch:
jobs: {}
